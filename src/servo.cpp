#include "pinout.h"
#include "Arduino.h"

void servoSetup() {
    // put code that only needs to run once here
}

void servoLoop() {
    // put code that needs to run continuously here (called every time from main's loop function)

    // Step 1: Write code to control the servos

    // End of spring break: Make a program that moves the TVC mount in circles

}